---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import VerticalFlow from "../../components/VerticalFlow.astro";

const all = await getCollection("bioregions");
const vistula =
  all.find((b) => b.data.slug === "vistula-pl") ||
  all.find((b) => b.data.name.toLowerCase().includes("vistula"));

const base = import.meta.env.BASE_URL.endsWith("/") ? import.meta.env.BASE_URL : import.meta.env.BASE_URL + "/";
const data = vistula?.data;
---

<BaseLayout title="Vistula — vertical layout test">
  <a class="back-link" href={base} aria-label="Back to the map">← Map</a>
  {data ? (
    <VerticalFlow
      bioregionName={data.name}
      bioregionSlug={data.slug}
      bioregionDescription={data.shortNarrative || data.intro}
      bioregionCountry={data.country}
      bioregionImageUrl={data.imageUrl}
      bioregionImageFile={data.imageFile}
      shifts={data.criticalShifts}
      innovations={data.systemicInnovations}
    />
  ) : (
    <p class="test-note">Vistula bioregion not found in the current data.</p>
  )}
  <p class="test-note">
    Test layout — vertical: bioregion on top (image left), connected by a bus tree to its critical shifts; each shift is a group with its innovations flowing horizontally to the right (image on top of each project card).
  </p>
</BaseLayout>

<style>
  .back-link {
    position: fixed;
    top: 16px;
    left: 16px;
    z-index: 20;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 14px;
    border-radius: 999px;
    border: 1px solid var(--line-strong);
    background: var(--paper-raised);
    color: var(--bwl-primary);
    font-family: var(--font-body);
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    box-shadow: 0 1px 4px rgba(32, 64, 79, 0.12);
    transition: background 0.15s ease, border-color 0.15s ease;
  }
  .back-link:hover {
    background: var(--bwl-primary);
    color: #fff;
    border-color: var(--bwl-primary);
  }
  .test-note {
    max-width: 1080px;
    margin: 1.5rem auto 0;
    padding: 0 1.5rem;
    text-align: center;
    color: var(--mist);
    font-family: var(--font-body);
    font-size: 13px;
  }
</style>
